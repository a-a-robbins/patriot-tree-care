# Copy to server/.env for local development only (gitignored).
# Production values: GitHub repo → Settings → Secrets and variables → Actions.

SMTP2GO_API_KEY=
QUOTE_FROM_EMAIL=
QUOTE_TO_EMAIL=

# Optional
SITE_NAME=Patriot Tree Care
PORT=3001
# CORS_ORIGIN=
